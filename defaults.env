# Global defaults for server bootstrap
# This is the SINGLE SOURCE OF TRUTH for default values.
# All scripts derive from this file — do NOT hardcode these values elsewhere.
#
# Override by setting environment variables before running bootstrap,
# or by creating a local override file at /${NAMESPACE}/config/local.env

# Root namespace (absolute path where everything lives)
NAMESPACE="${NAMESPACE:-/atn}"

# GitHub organization
GH_ORG="${GH_ORG:-atnplex}"

# Docker bridge network name
DOCKER_NETWORK="${DOCKER_NETWORK:-atn_bridge}"

# Log directory (derived from namespace)
LOG_DIR="${LOG_DIR:-${NAMESPACE}/logs}"

# Setup repo location (derived from namespace)
SETUP_REPO_DIR="${SETUP_REPO_DIR:-${NAMESPACE}/github/setup}"
